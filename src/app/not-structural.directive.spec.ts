import { NotStructuralDirective } from './not-structural.directive';

describe('NotStructuralDirective', () => {
  it('should create an instance', () => {
    const directive = new NotStructuralDirective();
    expect(directive).toBeTruthy();
  });
});
